default: &default
  adapter: postgresql
  encoding: unicode
  pool: 5
  host: <%= ENV["DB_HOST"] %>
  username: <%= ENV["DB_USERNAME"] %>
  password: <%= ENV["DB_PASSWORD"] %>


development:
  primary:
    <<: *default
    database: quadrovagas-development
  queue:
    <<: *default
    database: quadrovagas-queue

test:
  primary:
    <<: *default
    database: quadrovagas-test
  queue:
    <<: *default
    database: quadrovagas-queue

production:
  <<: *default
  database: quadrovagas-production